# Metadados da versão

- `manifest.json`: descrição estruturada do conjunto publicado.
- `validation_report.json`: verificações executadas antes da disponibilização.
- `checksums.sha256`: hashes SHA-256 dos arquivos versionados.

Para verificar a integridade:

```bash
python -m worcap_endmembers verify
```

